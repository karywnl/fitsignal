numpy>=1.26
sentence-transformers>=3.0
gradio==4.44.0
# Gradio 4.44 imports HfFolder from huggingface_hub, which was removed in hub 1.0.
# Pin below 1.0 (still compatible with sentence-transformers 3.x).
huggingface_hub>=0.23,<1.0
# audioop was removed from the Python 3.13 stdlib; backport guard if on 3.13+.
audioop-lts; python_version >= "3.13"
